---
layout: post
title: "前端性能"
categories: frontend
tag: [其它]
---

主要讲解前端性能相关知识，工具链，持续集成，搭建系统和前端架构

## 前端性能

- 从域名到 IP 地址，需要用 DNS 协议查询；
- HTTP 协议是用 TCP 传输的，所以会有 TCP 建立连接过程；
- 如果使用 HTTPS，还有有 HTTPS 交换证书；
- 每个网页还有图片等请求。

从这个分析和实际试验的结果看，网页的加载时间，不但跟体积有关系，还跟请求数有很大关系，因此，我们最终设计的技术方案大约可以这样划分

![技术方案划分](/assets/imgs/posts/performance-1.jpg)

Performance API

## 工具链

前端工作：

1. 初始化项目；
2. 运行和调试；
3. 测试（单元测试）；
4. 发布。

## 持续集成

1. 持续集成
2. 独立开发

前端的持续集成的措施应该是这样的：

1. 预览环境，代替每日构建，前端每次（或指定次）提交代码到仓库都同步到预览环境，保证预览环境总是可用；
2. 规则校验，代替构建验证测试，通过数据采集（如前面提到的性能数据）和代码扫描，保证提交的代码满足一定的质量要求。

## 搭建系统

### 设计

1. 模版化搭建
2. 模块化搭建
3. 数据驱动界面

模版，数据，模块，页面，上线

## 前端架构

框架：
第一代前端框架（如 jQuery, PrototypeJS）重点解决了浏览器兼容问题和 API 的易用性问题。
第二代前端框架（如 Vue，Angular，React）重点解决了组件化问题。选择合适的框架，可以节约架构的成本，还能够享受社区资源。

### 组件化

- Web Component；
- Vue；
- React；
- Angular；
- 自研。

### 兼容性和是配性

适配问题主要适配的是屏幕的三个要素。

- 单位英寸像素数（Pixel Per Inch，PPI）：现实世界的一英寸内像素数，决定了屏幕的显示质量。主要影响文字，可以采用media规则来适配。
- 设备像素比率（Device Pixel Ratio，DPR）：物理像素与逻辑像素（px）的对应关系。可以使用CSS的viewport规则解决。
- 分辨率（Resolution）：屏幕区域的宽高所占像素数。可以使用vw单位解决。




